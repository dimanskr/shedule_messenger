{% extends 'mailing/base.html' %}
{% load blog_tags %}
{% block content %}
    <div class="pricing-header px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center">
        <h1 class="display-4">Главная страница</h1>
    </div>
    <div class="row mb-3 text-center">
        <div class="col">
            <div class="card card-hover card-active mb-4 rounded-3 border-success">
                <div class="card-header py-3 text-bg-success border-success">
                    <h4 class="my-0 fw-normal">Количество рассылок всего</h4>
                </div>
                <div class="card-body text-bg-light rounded-3">
                    <h2 class="card-title">{{ total_mailings }}</h2>
                </div>
            </div>
        </div>

        <div class="col">
            <div class="card card-hover card-active mb-4 rounded-3 border-primary">
                <div class="card-header py-3 text-bg-success border-primary">
                    <h4 class="my-0 fw-normal">Количество активных рассылок</h4>
                </div>
                <div class="card-body text-bg-light rounded-3">
                    <h2 class="card-title">{{ active_mailings }}</h2>
                </div>
            </div>
        </div>

        <div class="col">
            <div class="card card-hover card-active mb-4 rounded-3 border-info">
                <div class="card-header py-3 text-bg-success border-info">
                    <h4 class="my-0 fw-normal">Количество уникальных клиентов</h4>
                </div>
                <div class="card-body text-bg-light rounded-3">
                    <h2 class="card-title">{{ unique_clients }}</h2>
                </div>
            </div>
        </div>
    </div>

    <div class="row mb-3 text-center">
        <div class="pricing-header px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center">
            <h2 class="display-4">Случайные статьи</h2>
        </div>
        <div class="row mb-2">
            {% for article in random_blog_posts %}
                <div class="col-md-4">
                    <div class="row g-0 border rounded overflow-hidden flex-md-row mb-4 shadow-sm h-md-250 position-relative">
                        <div class="col p-4 d-flex flex-column position-static">
                            <h3 class="mb-0">{{ article.title }}</h3>
                            <div class="mb-1 text-muted">{{ article.created_at | date:"d.m.Y" }}</div>
                            <p class="card-text mb-auto">{{ article.body | truncatechars:250 }}</p>
                            <a href="{% url 'blog:article_detail' article.slug %}" class="stretched-link">Продолжить
                                чтение</a>
                        </div>
                        <div class="col-auto d-none d-lg-block">
                            <img src="{{ article.preview | media_filter }}"
                                 alt="Article">
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}