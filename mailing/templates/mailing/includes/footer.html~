{% load mailing_tags %}
<div class="row">
    <div class="col-12 col-md">
        Shedule messenger
        <small class="d-block mb-3 text-muted">&copy; {% current_time %}</small>
    </div>
    <div class="col-6 col-md">
        <h5>Меню сайта</h5>
        <ul class="list-unstyled text-small">
            <li><a class="text-muted" href="{% url 'homepage:home' %}">Главная</a></li>
            {% if user.is_superuser or user.is_authenticated and not perms.mailing.can_watch_mailing %}
                <li><a class="text-muted" href="{% url 'mailing:client_list' %}">Список клиентов</a></li>
                <li><a class="text-muted" href="{% url 'mailing:message_list' %}">Список сообщений</a></li>
            {% endif %}
            {% if user.is_authenticated %}
                <li><a class="text-muted" href="{% url 'mailing:mailing_list' %}">Список рассылок</a></li>
                <li><a class="text-muted" href="{% url 'mailing:mailing_attempt_list' %}">Лог рассылок</a></li>
            {% endif %}
            <li><a class="text-muted" href="{% url 'blog:articles' %}">Блог</a></li>
        </ul>
    </div>
</div>